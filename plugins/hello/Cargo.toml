[workspace]

[package]
name = "hello"
version = "0.1.0"
edition = "2021"
authors = ["hpeXt"]
description = "Hello World plugin for minimal kernel"

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
